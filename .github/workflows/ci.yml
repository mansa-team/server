name: CI

on:
  push:
    branches: [ dev, main ]
  pull_request:
    branches: [ dev, main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-lint-${{ hashFiles('requirements.txt') }}
          
      - name: Install Ruff
        run: pip install ruff
        
      - name: Run Ruff
        run: ruff check main/ tests/

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          
      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('requirements.txt') }}
          
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov
          
      - name: Run Tests with Coverage
        run: pytest --cov=main --cov-report=xml --cov-report=term-missing
        env:
          PYTHONPATH: .
          
      - name: Upload coverage to Codecov
        if: github.event_name == 'pull_request'
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          fail_ci_if_error: false
          
      - name: Check Coverage Threshold
        run: |
          pip install coverage
          COVERAGE=$(python -c "import xml.etree.ElementTree as ET; tree = ET.parse('coverage.xml'); root = tree.getroot(); print(int(float(root.get('line-rate', 0)) * 100))")
          echo "Coverage: $COVERAGE%"
          if [ "$COVERAGE" -lt 70 ]; then
            echo "Coverage $COVERAGE% is below 70% threshold"
            exit 1
          fi
          echo "Coverage check passed!"

  docker-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: docker build -t mansa-server:test .

      - name: Run container in background
        run: |
          docker run -d --name mansa-test -p 8000:8000 \
            -e USER_MYSQL_HOST=${{ secrets.USER_MYSQL_HOST }} \
            -e USER_MYSQL_USER=${{ secrets.USER_MYSQL_USER }} \
            -e USER_MYSQL_PASSWORD=${{ secrets.USER_MYSQL_PASSWORD }} \
            -e USER_MYSQL_DATABASE=${{ secrets.USER_MYSQL_DATABASE }} \
            -e STOCKS_MYSQL_HOST=${{ secrets.STOCKS_MYSQL_HOST }} \
            -e STOCKS_MYSQL_USER=${{ secrets.STOCKS_MYSQL_USER }} \
            -e STOCKS_MYSQL_PASSWORD=${{ secrets.STOCKS_MYSQL_PASSWORD }} \
            -e STOCKS_MYSQL_DATABASE=${{ secrets.STOCKS_MYSQL_DATABASE }} \
            mansa-server:test

      - name: Wait for app to start
        run: sleep 20

      - name: Check health endpoint
        run: |
          curl -f http://localhost:8000/health || curl -f http://localhost:3200/user/health || echo "Checking health..."
          docker logs mansa-test | tail -20

      - name: Stop and remove container
        if: always()
        run: |
          docker stop mansa-test || true
          docker rm mansa-test || true